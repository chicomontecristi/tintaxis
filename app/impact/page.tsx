import ImpactClient from "./ImpactClient";

export const metadata = {
  title: "Impact — Tintaxis",
  description: "The environmental case for digital publishing. Every printed book has a price the reader never sees.",
};

export default function ImpactPage() {
  return <ImpactClient />;
}
